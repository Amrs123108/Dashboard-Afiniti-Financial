/** @type {import('next').NextConfig} */
const nextConfig = {
  // SheetJS se ejecuta solo en API routes (runtime nodejs); nada especial que configurar.
  reactStrictMode: true,
};

export default nextConfig;
